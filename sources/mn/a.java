package mn;

import com.transsion.mpush.api.LocalPushMessage;
import java.util.List;
import kotlin.coroutines.Continuation;

/* loaded from: classes6.dex */
public interface a {
    Object a(Continuation continuation);

    Object b(List list, Continuation continuation);

    Object c(String str, int i11, Continuation continuation);

    Object d(Continuation continuation);

    Object e(Continuation continuation);

    Object f(LocalPushMessage localPushMessage, Continuation continuation);

    Object g(String str, Continuation continuation);
}
